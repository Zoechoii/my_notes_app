<!DOCTYPE html>
<html>
<head>
    <title>Search result</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <!-- keyword와 notes 변수는 라우터에서 전달받음 -->
    <h1>🔍 '<%= keyword %>' Search result</h1>
    
    <p><a href="/notes/search" class="btn">🔍 다시 검색</a></p>
    
    <div class="notes-list">
        <!-- 검색 결과 없을 때 -->
        <% if (notes.length === 0) { %>
            <p class="no-notes">No results. 😢</p>
        
        <!-- 검색 결과 있을 때 -->
        <% } else { %>
            <p><%= notes.length %>: Search result</p>
            
            <!-- 각 노트 보여주기 (list.ejs와 비슷) -->
            <% notes.forEach(function(note) { %>
                <div class="note-card">
                    <h3>
                        <a href="/notes/view/<%= note.id %>">
                            <%= note.title %>
                        </a>
                    </h3>
                    
                    <p class="preview">
                        <%= note.content.substring(0, 100) %>
                        <% if (note.content.length > 100) { %>...more<% } %>
                    </p>
                    
                    <small class="date">
                        마지막 수정: <%= new Date(note.updated_at).toLocaleString('ko-KR') %>
                    </small>
                    
                    <div class="actions">
                        <a href="/notes/view/<%= note.id %>" class="btn-small">👁️ View</a>
                        <a href="/notes/edit/<%= note.id %>" class="btn-small">✏️ Fix</a>
                        <a href="/notes/delete/<%= note.id %>" 
                           class="btn-small btn-danger"
                           onclick="return confirm('Are you sure to delete this note?')">
                            🗑️ Delete
                        </a>
                    </div>
                </div>
            <% }); %>
        <% } %>
    </div>
    
    <p><a href="/notes/list">📚 list</a></p>
</body>
</html>